#!/usr/bin/perl -wn
# Print out the estimated true mean of a set of data read from standard in

use FindBin qw($Bin);
use lib "$Bin/lib/";
use VMR::Stat;

$x[$i++] = $_;
END {
	$i = 0;
	print calc_true_mean(99, 1, @x);
}
