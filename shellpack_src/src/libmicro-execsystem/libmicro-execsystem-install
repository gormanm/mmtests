#!/bin/bash
# libmicro installer
###SHELLPACK preamble libmicro-execsystem-install 1
GIT_LOCATION=https://github.com/redhat-performance/libMicro
MIRROR_LOCATION="$WEBROOT/libmicro"

install-depends gcc

###SHELLPACK parseargBegin
###SHELLPACK parseargEnd

###SHELLPACK git_fetch libmicro-execsystem-${VERSION}.tar.gz libmicro-execsystem-${VERSION}-installed

###SHELLPACK build_start libmicro-execsystem-${VERSION}-installed

###SHELLPACK make_clean
###SHELLPACK make

echo libmicro installed successfully
