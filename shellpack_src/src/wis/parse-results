#!/bin/bash
SHELLPACK_ROOT=$(dirname $(realpath -s ${BASH_SOURCE[0]}))
SHELLPACK_BIN=$SHELLPACK_ROOT/../../parsers

LOGDIR=${1:-`pwd`}
cd $LOGDIR || exit 1
$SHELLPACK_BIN/line-parse-results `pwd` 'total:' ops 3 | sed -e 's/total://'
