#!/bin/bash
SHELLPACK_ROOT=$(dirname $(realpath -s ${BASH_SOURCE[0]}))
SHELLPACK_BIN=$SHELLPACK_ROOT/../../parsers

LOGDIR=${1:-`pwd`}
cd $LOGDIR || exit 1
export PARSE_EXCLUDE_USER_TIME=yes
$SHELLPACK_BIN/time-parse-results `pwd`
